<h4 class="bg-success text-center text-white pb-2"><%= @category.name %>: $<%= @category.category_total_amount %></h4>
</div>
<section>
  <div class="container mb-5">
    <%= render partial: "transaction", collection: @transactions %>
  </div>
  <%= link_to "Add New Transaction", new_transaction_path, class:"btn btn-success rounded-0 w-100 position-fixed bottom-0" %>
<section>
